
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <stdint.h>

typedef struct {
    enum {

    } type;
    union {

    } data;
} token;

typedef struct {

} token_state;
